# Socket Painting Canvas Production Environment Configuration
# Copy this file to config.prod.env and update the values

# Server Configuration
PORT=3000
NODE_ENV=production

# Production URLs (replace with your actual domain)
CLIENT_PROD_URL=https://yourdomain.com
SERVER_URL=https://yourdomain.com

# Socket.IO Configuration for production
SOCKET_CORS_ORIGIN=https://yourdomain.com

# Logging
LOG_LEVEL=warn

# Maximum Users
MAX_USERS=50

# Security
# Generate a strong random secret with: openssl rand -base64 32
# NEVER commit the actual secret to version control
SESSION_SECRET=REPLACE_WITH_SECURE_RANDOM_SECRET
