# Socket Painting Canvas Production Environment Configuration

# Server Configuration
PORT=3000
NODE_ENV=production

# Production URLs
CLIENT_PROD_URL=https://paint.wilbertopachecob.dev
SERVER_URL=https://paint.wilbertopachecob.dev

# Socket.IO Configuration for production
SOCKET_CORS_ORIGIN=https://paint.wilbertopachecob.dev

# Logging
LOG_LEVEL=warn

# Maximum Users
MAX_USERS=50

# Security
# SESSION_SECRET must be set externally during deployment (e.g., via environment variables or a secret manager).
# Generate a secure secret with: openssl rand -base64 32
# DO NOT set SESSION_SECRET in this file - it will be validated during startup

# Deployment Configuration (Optional - set these during deployment)
# DEPLOY_USER=your-deploy-username
# DEPLOY_HOST=your-server-ip-or-domain
# DEFAULT_BRANCH=master
